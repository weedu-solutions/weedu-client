cd /home/ubuntu/weedu-client # Navega até o diretório da aplicação, onde o código fonte está localizado.
sudo git pull --rebase # Realiza um comando "git pull --rebase" para atualizar o repositório Git do cliente.
sudo docker-compose build # Usa o Docker Compose para construir os contêineres definidos no arquivo "docker-compose.yml".
sudo docker-compose up -d # Inicia os contêineres definidos no arquivo "docker-compose.yml" em segundo plano (-d).
